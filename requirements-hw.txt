# Hardware dependencies — only needed for real transmission through Pluto+.
# libiio (the native library) must be installed separately:
#   Windows:  the libiio installer (Analog Devices)
#   Linux:    apt install libiio0 libiio-utils   (or build from source)
#
# Python wrappers:
pyadi-iio>=0.0.16
pylibiio>=0.24

# --- SoapySDR (backend=soapy): HackRF, LimeSDR, BladeRF, USRP, etc. ---
# SoapySDR is NOT installable via pip — install it system-wide together with the
# device driver:
#   Windows: PothosSDR (https://github.com/pothosware/PothosSDR) — ships SoapySDR,
#            the Python bindings and the drivers (hackrf/lime/uhd/bladerf). Add its
#            Python to PATH or copy SoapySDR into the venv.
#   Linux:   apt install python3-soapysdr soapysdr-module-hackrf
#            (or -lime / -uhd / -bladerf / -plutosdr for your SDR)
#   macOS:   brew install soapysdr    (+ the driver formula, e.g. soapyhackrf)
