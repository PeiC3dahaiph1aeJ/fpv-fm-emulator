# Апаратні залежності — лише для реальної передачі через Pluto+.
# libiio (нативна бібліотека) має бути встановлена окремо:
#   Windows:  інсталятор libiio (Analog Devices), уже присутній у системі
#   Linux:    apt install libiio0 libiio-utils   (або збірка з джерел)
#
# Python-обгортки:
pyadi-iio>=0.0.16
pylibiio>=0.24

# --- SoapySDR (backend=soapy): HackRF, LimeSDR, BladeRF, USRP тощо ---
# SoapySDR НЕ ставиться через pip — постав системно разом із драйвером пристрою:
#   Windows: PothosSDR (https://github.com/pothosware/PothosSDR) — містить SoapySDR,
#            Python-біндинги й драйвери (hackrf/lime/uhd/bladerf). Додай його Python
#            до PATH або скопіюй SoapySDR у venv.
#   Linux:   apt install python3-soapysdr soapysdr-module-hackrf
#            (або -lime / -uhd / -bladerf / -plutosdr під свій SDR)
#   macOS:   brew install soapysdr    (+ формула драйвера, напр. soapyhackrf)
