name: "Static carrier R1"
type: static

signal:
  standard: PAL50
  pattern: bars
  sample_rate: 8.0e6
  deviation_pp_hz: 6.0e6
  gain_db: -10          # tx_hardwaregain: 0 = max, -89 = min

static:
  channel: R1           # 5658 MHz; or set freq_mhz: 5658
  hold_s: 0             # 0 = hold until stopped (Ctrl+C)
